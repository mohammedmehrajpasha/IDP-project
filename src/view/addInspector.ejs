<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Inspector – FSSAI Inspector Hub</title>
  <link rel="stylesheet" href="/css/addInspector.css" />
</head>
<body>

<%- include('partials/header') %>

<section class="features">
  <div class="container">
    <div class="features-heading centered">
      <h2>Add New Inspector</h2>
      <p>Enter details for the inspector to be assigned under your zone.</p>
    </div>
    <% if (success) { %>
      <div class="success-message">
        ✅ Inspector added successfully!
      </div>
    <% } %>

    <!-- Add Inspector Form -->
    <form action="/admin/inspectors/add" method="POST" class="add-form">
      <input type="text" name="name" placeholder="Inspector Name" required>
      <input type="email" name="email" placeholder="Email" required>
      <input type="text" name="phone" placeholder="Phone Number" required>
      <input type="text" name="region" placeholder="Region" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit" class="btn">Add Inspector</button>
    </form>
    
    <div class="centered" style="margin-top: 20px;">
      <a href="/admin/inspectors" class="btn btn-outline">Back to Inspector List</a>
    </div>
  </div>
</section>

<%- include('partials/footer') %>
