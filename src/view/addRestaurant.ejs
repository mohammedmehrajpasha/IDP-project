<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Restaurant – FSSAI Inspector Hub</title>
  <link rel="stylesheet" href="/css/addRestaurant.css" />
</head>
<body>

<%- include('partials/header1') %>

<section class="features">
  <div class="container">
    <div class="features-heading centered">
      <h2>Add New Restaurant</h2>
      <p>Enter the restaurant details. Zone and region are auto-assigned based on your profile.</p>
    </div>
    <% if (success) { %>
      <div class="success-message">
        ✅ Restaurant added successfully!
      </div>
    <% } %>

    <form action="/inspector/restaurants/add" method="POST" class="add-form">
      <input type="text" name="name" placeholder="Restaurant Name" required />
      <input type="text" name="license_number" placeholder="License Number" required />
      <input type="email" name="email" placeholder="Email" required />
      <input type="text" name="phone" placeholder="Phone Number" required />
      <textarea name="address" placeholder="Restaurant Address" rows="3" required></textarea>

      <!-- Zone and region shown but not editable -->
      <input type="text" value="<%= zone %>" disabled />
      <input type="text" value="<%= region %>" disabled />

      <button type="submit" class="btn">Add Restaurant</button>
    </form>


    

    <div style="margin-top: 20px; text-align: center;">
      <a href="/inspector/dashboard" class="btn btn-outline">Back to Dashboard</a>
    </div>
  </div>
</section>

<%- include('partials/footer') %>
