<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>🍽️ Restaurants & Inspections</title>
  <link rel="stylesheet" href="/css/manageRestaurants.css" />
</head>
<body>

<%- include('partials/header1') %>

<section class="features">
  <div class="container">
    <div class="features-heading centered">
      <h2>🍽️ Restaurants & Inspections</h2>
      <p>Monitor hygiene and schedule inspections for restaurants in your zone.</p>
    </div>
    <% if (success) { %>
      <div class="success-message">
        ✅ Inspection Scheduled successfully!
      </div>
    <% } %>

    <div class="inspection-table">
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>License #</th>
            <th>Region</th>
            <th>Last Inspection</th>
            <th>Hygiene Score</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% approvedRestaurants.forEach(r => { %>
            <tr>
              <td><%= r.name %></td>
              <td><%= r.license_number %></td>
              <td><%= r.region %></td>
              <td><%= r.last_inspection_date ? new Date(r.last_inspection_date).toLocaleDateString() : 'N/A' %></td>
              <td><%= r.hygiene_score || 'N/A' %></td>
              <td>

                <a href="/admin/restaurants/schedule/<%= r.id %>" class="btn">Schedule</a>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>

    <div class="centered">
      <a href="/admin/inspections" class="btn btn-outline">← Go Back</a>
    </div>
  </div>
</section>

<%- include('partials/footer') %>
