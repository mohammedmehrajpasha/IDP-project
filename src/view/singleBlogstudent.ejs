<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= blog.title %> - Blog Details</title>
  <link rel="stylesheet" href="/css/singleBlog.css" />
</head>
<body>

  <%- include('partials/header') %>

  <div class="blog-container">
    <div class="blog-post">
      <h1><%= blog.title %></h1>
      By <%= blog.author_name %> | <%= blog.formatted_date %> at <%= blog.formatted_time %>
      <div class="blog-content">
        <p><%= blog.description %></p>
      </div>
    </div>
  
    <div class="comments-section">
        <h2>Comments</h2>
        <% if (comments.length === 0) { %>
          <p>No comments yet. Be the first to comment!</p>
        <% } else { %>
            <% comments.forEach(comment => { %>
                <div class="comment">
                  <p><%= comment.commenter_name %> </p>
                  <p><%= comment.comment_text %></p>
                </div>
              <% }) %>
        <% } %>
  
        <form class="comment-form" method="POST" action="/submit-comment-studentblog">
            <input type="hidden" name="blog_id" value="<%= blog.id %>" />
            <input type="hidden" name="commenter_name" value="<%= nameinfo.name %>" />
            <input type="hidden" name="alumni_id" value="<%= nameinfo.id %>" />
          
            <textarea name="comment_text" rows="4" placeholder="Your comment" required></textarea>
            <button type="submit">Post Comment</button>
          </form>
    </div>
  </div>
  

  <%- include('partials/footer') %>

</body>
</html>
