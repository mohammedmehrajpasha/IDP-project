<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Resolve Complaint</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/pendingComplaints.css">
</head>
<body>
  <div class="container my-5">
    <h2 class="text-center mb-4">Complaint Resolution</h2>

    <form action="/authority/resolved/<%= complaint.id %>" method="POST">
      <div class="mb-3">
        <label class="form-label"><strong>Type of Complaint:</strong></label>
        <p class="form-control-plaintext"><%= complaint.type %></p>
      </div>

      <div class="mb-3">
        <label class="form-label"><strong>Subject Description:</strong></label>
        <p class="form-control-plaintext"><%= complaint.subject_description %></p>
      </div>

      <div class="mb-3">
        <label class="form-label"><strong>School ID:</strong></label>
        <p class="form-control-plaintext"><%= complaint.school_id %></p>
      </div>

      <div class="mb-3">
        <label class="form-label"><strong>Date of Incident:</strong></label>
        <p class="form-control-plaintext"><%= complaint.date_of_incident.toISOString().split('T')[0] %></p>
      </div>

      <div class="mb-3">
        <label class="form-label"><strong>Expected Outcome:</strong></label>
        <p class="form-control-plaintext"><%= complaint.expected_outcome %></p>
      </div>

      <div class="mb-3">
        <label for="resolution" class="form-label"><strong>Resolution Taken:</strong></label>
        <textarea class="form-control" id="resolution" name="resolution" rows="4" placeholder="Describe how the issue has been addressed..." required></textarea>
      </div>

      <div class="text-center">
        <button type="submit" class="btn btn-success px-5">Resolve</button>
      </div>
    </form>
  </div>
</body>
</html>