<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Submit a Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<body class="bg-light">
  <%- include('partials/header') %>
  <div class="container mt-5">
    <h2 class="mb-4">📝 Submit Your Blog</h2>
    <% if (success) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        Blog submitted successfully!
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
      <% } %>

    <form action="/submit-blog" method="POST">
      <!-- Blog Title -->
      <div class="mb-3">
        <label for="title" class="form-label">Blog Title</label>
        <input type="text" class="form-control" id="title" name="title" required>
      </div>

      <!-- Blog Description -->
      <div class="mb-3">
        <label for="description" class="form-label">Description</label>
        <textarea class="form-control" id="description" name="description" rows="6" required></textarea>
      </div>

      <!-- Alumni ID (Hidden or from session) -->
      <!-- <div class="mb-3">
        <label for="uploaded_date" class="form-label">Date</label>
      <input type="number" name="alumni_id" required>
    </div> -->

      <!-- Uploaded Date (default to today) -->
      <div class="mb-3">
        <label for="uploaded_date" class="form-label">Date</label>
        <input type="date" class="form-control" id="uploaded_date" name="uploaded_date" required>
      </div>

      <!-- Submit Button -->
      <button type="submit" class="btn btn-primary">Submit Blog</button>
    </form>
  </div>
  <!-- Bootstrap JS (with Popper) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>


 
  <%- include('partials/footer') %>

</body>
</html>
