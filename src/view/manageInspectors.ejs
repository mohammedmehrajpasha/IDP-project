<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Manage Inspectors</title>
  <link rel="stylesheet" href="/css/manageInspectors.css" />
</head>
<body>

<%- include('partials/header1') %>

<section class="features">
  <div class="container">
    <div class="features-heading">
      <h2 style="color: black;">Manage Inspectors</h2>
      <p>View, edit, and manage inspectors for your assigned zone.</p>
    </div>

    <!-- Zone and Add Inspector Row -->
    <div class="zone-header">
      <div >
        <strong>Zone :</strong> <%= zone %>
      </div>
      <div class="add-button">
        <a href="/admin/inspectors/add" class="btn">Add Inspector</a>
      </div>
    </div>

    <!-- Inspectors Table -->
<div class="inspection-table">
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Email</th>
        <th>Phone</th>
        <th>Region</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% inspectors.forEach(inspector => { %>
        <tr>
          <td><%= inspector.id %></td>
          <td><%= inspector.name %></td>
          <td><%= inspector.email %></td>
          <td><%= inspector.phone %></td>
          <td><%= inspector.region %></td>
          <td>
            <a href="/admin/inspectors/edit/<%= inspector.id %>" class="btn btn-outline">
              Edit
            </a>
            <form action="/admin/inspectors/delete/<%= inspector.id %>" method="POST" style="display:inline;">
              <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this inspector?')">
                Delete
              </button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>
  </div>
</section>

<%- include('partials/footer') %>
