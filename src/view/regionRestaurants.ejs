<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Regional Restaurants – FSSAI Inspector Hub</title>
  <link rel="stylesheet" href="/css/home1.css" />
  <style>
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 30px;
    }
    th, td {
      border: 1px solid #ccc;
      padding: 12px;
      text-align: left;
    }
    th {
      background-color: #f2f2f2;
    }
    tr:hover {
      background-color: #f9f9f9;
    }
    .btn-view {
      background-color: #007bff;
      color: white;
      padding: 6px 12px;
      text-decoration: none;
      border-radius: 5px;
    }
    .btn-view:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <section class="features">
    <div class="container">
      <h2>📍 Restaurants in Your Zone – <%= inspectorName %></h2>

      <% if (restaurants.length === 0) { %>
        <p>No restaurants found in your zone.</p>
      <% } else { %>
        <table>
          <thead>
            <tr>
              <th>🍽️ Name</th>
              <th>📄 License No.</th>
              <th>📍 Region</th>
              <th>🗓️ Last Inspection</th>
              <th>🔍 Action</th>
            </tr>
          </thead>
          <tbody>
            <% restaurants.forEach(restaurant => { %>
              <tr>
                <td><%= restaurant.name %></td>
                <td><%= restaurant.license_number %></td>
                <td><%= restaurant.region %></td>
                <td>
                  <% if (restaurant.last_inspection_date) { %>
                    <%= new Date(restaurant.last_inspection_date).toLocaleDateString() %>
                  <% } else { %>
                    ⏳ Not Inspected
                  <% } %>
                </td>
                <td>
                  <a href="/inspector/restaurants/<%= restaurant.id %>" class="btn-view">View</a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>
  </section>

  <%- include('partials/footer') %>
</body>
</html>
