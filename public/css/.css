body {
    font-family: Arial, sans-serif;
    background-color: #f5f7fa;
    margin: 0; 
    padding: 0;
    color: #333;
  }
  
  header {
    background-color: #003366;
    color: white;
    padding: 1rem 2rem;
    text-align: center;
  }
  
  .container {
    max-width: 900px;
    margin: 2rem auto;
    padding: 0 1rem;
  }
  
  input[type="search"] {
    width: 100%;
    font-size: 1.2rem;
    padding: 0.7rem 1rem;
    border-radius: 4px;
    border: 1px solid #ccc;
    margin-bottom: 1.5rem;
    box-sizing: border-box;
  }
  
  h2 {
    color: #003366;
    margin-top: 2rem;
    margin-bottom: 1rem;
  }
  
  table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 1.5rem;
  }
  
  th, td {
    border: 1px solid #ddd;
    padding: 0.5rem 1rem;
    text-align: left;
  }
  
  th {
    background-color: #e0eafc;
  }
  
  .resource-list, .session-list, .suggestion-list {
    list-style: none;
    padding-left: 0;
  }
  
  .resource-list li, .session-list li, .suggestion-list li {
    background: white;
    margin-bottom: 0.8rem;
    padding: 1rem;
    border-radius: 6px;
    box-shadow: 0 1px 4px rgba(0,0,0,0.1);
  }
  
  .resource-type {
    font-weight: bold;
    color: #0066cc;
  }
  
  .session-time {
    font-style: italic;
    color: #555;
  }
  
  .no-results {
    color: #999;
    font-style: italic;
  }
  
  button.search-btn {
    background-color: #003366;
    color: white;
    border: none;
    padding: 0.6rem 1.2rem;
    border-radius: 4px;
    cursor: pointer;
    font-size: 1rem;
    margin-top: 0.5rem;
  }
  
  button.search-btn:hover {
    background-color: #00509e;
  }
  